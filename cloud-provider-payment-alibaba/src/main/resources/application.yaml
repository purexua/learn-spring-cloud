server:
  port: 9001

spring:
  application:
    name: cloud-provider-payment-alibaba
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719